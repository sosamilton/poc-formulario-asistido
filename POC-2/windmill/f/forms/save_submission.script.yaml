summary: Guardar submission de formulario
description: Crea o actualiza una submission de formulario
kind: script
language: bun
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    submission_id:
      type: string
      description: ID de submission existente (para actualizar)
    form_id:
      type: string
      description: ID del formulario
    version:
      type: integer
      description: Versión del formulario
    data:
      type: object
      description: Datos del formulario
    status:
      type: string
      description: Estado de la submission
      enum: [draft, submitted, completed, failed]
      default: draft
    validation_errors:
      type: object
      description: Errores de validación
    metadata:
      type: object
      description: Metadata adicional
    created_by:
      type: string
      description: Usuario que crea la submission
      default: anonymous
  required:
    - form_id
    - version
    - data
