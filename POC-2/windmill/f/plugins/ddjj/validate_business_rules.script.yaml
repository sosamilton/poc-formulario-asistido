summary: Validar reglas de negocio DDJJ
description: Valida reglas de negocio específicas para DDJJ IIBB
lock: '!inline f/plugins/ddjj/validate_business_rules.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    version:
      type: integer
      description: Versión del formulario
    data:
      type: object
      description: Datos del formulario a validar
    form_id:
      type: string
      description: ID del formulario
  required:
    - data
    - form_id
    - version
