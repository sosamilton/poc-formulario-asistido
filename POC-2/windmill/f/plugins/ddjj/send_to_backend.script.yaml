summary: Enviar a backend IIBB
description: Envía la submission al backend de IIBB
kind: script
language: bun
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    submission_id:
      type: string
      description: ID de la submission
    form_id:
      type: string
      description: ID del formulario
    data:
      type: object
      description: Datos del formulario
    metadata:
      type: object
      description: Metadata adicional
  required:
    - submission_id
    - form_id
    - data
