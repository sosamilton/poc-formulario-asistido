summary: Parsear token JWT y extraer CUIT
description: Decodifica el token JWT y extrae el CUIT del claim identifier
lock: '!inline f/ddjj/parse_jwt.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    token:
      type: string
      description: Token JWT desde request de webhook
      default: null
      originalType: string
  required:
    - token
