summary: Create Form.io submission with precargado data
description: Creates a draft submission in Form.io with all collected data
lock: '!inline f/ddjj/create_formio_submission.script.lock'
kind: script
schema:
  $schema: https://json-schema.org/draft/2020-12/schema
  type: object
  properties:
    data:
      type: object
      description: Complete submission data
      properties:
        actividad:
          type: string
        alicuota:
          type: number
        cuit:
          type: string
        montoAnterior:
          type: number
        montoMinimo:
          type: number
        periodosAdeudados:
          type: array
          items:
            type: string
        razonSocial:
          type: string
        regimen:
          type: string
  required:
    - data
